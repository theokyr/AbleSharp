<!-- File: AbleSharp.GUI/Views/ProjectView.axaml -->
<UserControl
    x:Class="AbleSharp.GUI.Views.ProjectView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:AbleSharp.GUI.ViewModels"
    xmlns:views="clr-namespace:AbleSharp.GUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="1000" d:DesignHeight="700">

    <Grid RowDefinitions="*" Margin="10">
        <!--
        <TreeView Grid.Row="0"
                  Height="10"
                  ItemsSource="{Binding RootTracks}">
            <TreeView.ItemTemplate>
                <TreeDataTemplate
                    DataType="vm:TrackViewModel"
                    ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <TextBlock Text="{Binding TrackName}" FontSize="14" />
                    </StackPanel>
                </TreeDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView> -->

        <views:TimelineView Grid.Row="0"
                            DataContext="{Binding TimelineVM}" />
    </Grid>
</UserControl>